[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['cd server && npm ci --only=production']

[phases.build]
cmds = []

[start]
cmd = 'cd server && npm start'

[providers]
nodejs = '20'
